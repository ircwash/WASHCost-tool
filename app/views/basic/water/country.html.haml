= content_for :title do
  Start - Basic Water Calculator

.question
  %h1.question--title= t 'basic.shared.questionnaire.country.question'

  .question--answer

    .question--image.icon--tool-globe

    = form_for @questionnaire, :url => basic_water_update_url( I18n.locale, 'technology' ), :html => { 'data-dynamic_form' => basic_water_dynamic_update_url( I18n.locale ) } do |f|

      .choices-select
        = f.select :country, options_for_select( options_for_countries, @questionnaire.country ), { :include_blank => t( 'basic.shared.questionnaire.country.placeholder' ) }, { :class=>"chzn-select" }

      .question--buttons
        = submit_tag t( 'buttons.next' ), :class => 'button'

.info_tab
  .info_tab--icon
  .info_tab--container
    .info_tab--content
      %p.info_tab--content--text= raw format_info_text 'basic.shared.questionnaire.country.info'
