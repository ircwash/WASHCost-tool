= content_for :inner_content do
  .boxed_container
    .page_content.boxed_content.boxed_content-higher_contrast

      = render 'tool/navigation'

      = yield

= render template: "layouts/calculators"