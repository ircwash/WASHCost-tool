<div class="span3">
    <div class="well sidebar-nav">
        <ul class="nav nav-list">
            <li class="nav-header">All contacts</li>
            {{#each question in controller}}
                {{question.numeric_reference}}
                {{question.caption}}
                {{question.section}}
                {{view App.QuestionView}}
            {{/each}}
        </ul>
    </div>
</div>
<div class="span9">
    {{outlet}}
</div>
